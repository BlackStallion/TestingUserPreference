<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="test.unit.cointribe.com.myapplication">

    <!-- internet -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LogIn"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".JSONSAMPLE"
            android:label="@string/title_activity_jsonsample"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".HomePage" />
        <activity android:name=".DevelopmentUserInstructionPage" />
        <activity android:name=".ProductPage" />
        <activity android:name=".FreeUserInstructionPage" />
        <activity android:name=".HomePageGold"></activity>
    </application>

</manifest>